import React from 'react';

const START_MOTION_AFTER_IDLE_DURATION = 1000

export default function useStopSlideOnMouseMove(motion: number) {
    // if motion is 0, start a countdown timer to restart the motion
    // if motion is non zero

    return (
        <></>
    )
}